# LLMLearning

用一系列**可交互 HTML 网页**系统学习大模型。每条学习线独立成一个中文命名的顶层文件夹，配索引目录 + 分节详解 + 可交互 demo + 参考文献（一手来源可点击溯源）。纯静态，浏览器直接打开。

## 学习线

| 学习线 | 主题 | 目录 | 状态 |
|---|---|---|---|
| **大模型基础课** | 分课讲基础理论。[第一课：Tokenizer 原理（方法时间线式）](大模型基础课/第一课-Tokenizer/)；[第二课：位置编码](大模型基础课/第二课-位置编码/) | [`大模型基础课/`](大模型基础课/) | 第一课 v3 · 第二课 v1 |
| **大模型演进路线** | 主流大模型（8 家）发展脉络站点 | [`大模型演进路线/`](大模型演进路线/) | v2.1（泳道时间线 + 发布×量级散点图 + 技术深挖页 + 指标逐点溯源） |

## 在线访问 / 部署

站点部署在服务器上，浏览器访问门户首页 [`index.html`](index.html)（本地打开或服务器 `http://<公网IP>:9501/`）即可进入三条学习线。

**推送即上线**：本地 `git push` 到 `main` 后，服务器定时（每 2 分钟）自动 `git` 跟随更新，无需手动登服务器。一次性部署步骤见 [`deploy/README.md`](deploy/README.md)（nginx 托管 9501 + systemd 定时拉取）。

## 快速开始

纯静态网页，无需起服务，直接用浏览器打开门户或各学习线首页即可：

```bash
open index.html
```

带交互动画的页面建议用本地静态服务打开以确保 JS 正常：

```bash
cd 大模型基础课/第一课-Tokenizer && python3 -m http.server 8791
# 浏览器访问 http://localhost:8791/index.html
```

## 第一课主线（Tokenizer）

> 词（word）→ 字符（char）→ 词片（subword：BPE/WordPiece/Unigram）→ 字节（byte-level BPE）→ 动态字节块（frontier）

核心结论：主流生成式大模型（GPT / Qwen / DeepSeek / Kimi K2）今天基本都用同一套 **字节级 BPE**，差异只在词表大小与预切分规则。详见 [第一课 README](大模型基础课/README.md)。

## 项目文档 / 协作

多窗口并行产出，规矩与留痕见 [`docs/`](docs/)：

- [协作规矩](docs/协作规矩.md) — 角色边界 / 目录约定 / Git 协作 / 文档规范 / 多窗口派发
- [开发日志](docs/日志/开发日志.md) — 每轮做了什么、为什么、踩过的坑（时间倒序）
- [计划](docs/计划/) · [调研](docs/调研/)
