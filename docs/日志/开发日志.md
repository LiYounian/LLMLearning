# LLMLearning 开发日志

> 时间倒序，最新在上。每完成一轮（开发 / 优化 / 修复 / 协调）就记一笔。
> 记"为什么"和"踩过的坑"，诚实写验证结果。细节太多的单轮可另开专题日志、这里留要点 + 链接。

---

## 2026-08-21 · 集成第一课增强 x2（交互 demo 扩充）

**做了什么**
- 集成 `feat/tokenizer@97227dd`（含 2a36b64，2 提交，16 文件）进 main：
  - m3-bpe 加真实 Qwen「文字→字节→切割」流水线着色可视化；
  - m7-bbpe 新增①中文字节级 BPE 训练器（演示为何中文需大词表）②"token 跨字符边界"实测（Qwen 韩语 토크=前字尾+后字头，脚本 `probe_crosschar.py`）；
  - m9-frontier 新增 BLT 熵分块交互动画 + glitch token 真实 ID 画廊。
- 课程状态维持第一课 v3（内部增强，未改根 README 入口）。

**验证**
- `git diff --name-only origin/main origin/feat/tokenizer`：16 文件全在 `大模型基础课/`（第一课目录 + 课级 README），不碰根 README/中央 docs/演进路线/第二课/gitignore → 干净 `--no-ff`。内容窗口自报：本地验证、无 console error、脱敏 clean。

**分支 / PR**
- 合并 `feat/tokenizer`（基于 a893e65）进 main，独立临时 worktree `--no-ff`。

---

## 2026-08-21 · 集成演进路线 v2（时间线公司泳道视图）

**做了什么**
- 集成演进路线 **v2**：合并 `feat/roadmap-8companies@fc6924a` 的时间线增量进 main——`timeline.html` 新增视图切换：原「按年份」+ 新「公司泳道」（行=9 家含行业共性道 / 列=年份，事件落交叉格，主题筛选联动、无命中泳道整条淡出）。只改 `大模型演进路线/{timeline.html, assets/timeline.js}`。
- 根 README：演进路线状态「v1 完成」→「v2（14 页 + 时间线公司泳道视图）」。

**关键决策 / 教训**
- 该分支基于旧 main（a847a1b）、落后当前 main，`merge-base --is-ancestor` 为否。但核验 fc6924a 单提交只改 `timeline.*`，与 main 后来的第一课 v3/docs 改动**文件不相交**，三方合并（base=b20acd7）自动干净、无冲突；合并后校验基础课 v3(m1-m9) 完好、timeline 已更新。
- 已提醒该窗口：后续按规范先 `git fetch` rebase 到最新 main 再推，让分支保持 ancestor、集成更干净（避免"落后分支反向合并"的历史噪音）。

**验证**
- `git diff --name-only b20acd7 fc6924a` = 仅 `timeline.html`+`timeline.js`；合并 `ort` 策略自动完成，无冲突。内容窗口自报浏览器 DOM 核实（9 道×9 列、MoE 筛选 7 高亮 32 淡出、无命中泳道淡出均正确）。

**分支 / PR**
- 合并 `feat/roadmap-8companies`（含 fc6924a）进 main，独立临时 worktree `--no-ff`；同集成更新 README + 本条日志。

---

## 2026-08-21 · 集成基础课第一课 v3 + 第二课(位置编码)立项 + 规矩细化

**做了什么**
- 集成基础课 **第一课 v3**：合并 `feat/tokenizer@f5481f5`（20 文件，+1202/-518）进 main。第一课从"章节式"重构为"方法时间线式"——首页 9 个方法节点按发布年可点击，拆出 word/char/BPE/WordPiece/Unigram/SentencePiece/字节级BPE/tiktoken大词表/前沿 共 9 个方法详解页，每页标注"被哪些主流模型使用"，新增 `models.html` 方法×模型矩阵。旧编号页(01-08)经 git rename/删除。验证（内容窗口自报）：14 页无死链、两个交互 demo 正常。
- 根 README：基础课状态 v2 → **v3（方法时间线式）**，并挂出第二课"位置编码（规划中）"。
- **第二课「位置编码」立项**：主题=位置编码专题（Sinusoidal→学习式绝对→相对(Shaw/T5)→RoPE→ALiBi→NoPE，按发布时间线排序 + 每方法标注采用模型），目录 `大模型基础课/第二课-位置编码/`，参考第一课 v3 的"方法时间线式"设计。新开一个专属窗口做，分支 `feat/lesson2-pos-encoding` + worktree 已备好。
- **细化协作规矩**：明确"根 README + 中央开发日志由统筹窗口维护"，内容窗口不直接改这两处、报统筹代记（避免共享文件撞车）；课内细节放各自课程目录 `NOTES.md`。

**关键决策 / 教训**
- 第一课 v3 的"方法时间线 + 每法标注使用模型"设计恰好就是用户想要第二课采用的样式——第二课直接复用这套设计模板，且两课内容（分词方法 vs 位置编码方法）不重叠、天然解耦（同为 `大模型基础课/` 下不同子目录）。
- 中央 README/日志的写者收敛到统筹一人，是本轮把"已发生过一次 README 撞车"的教训制度化。

**验证**
- `git diff --name-only 2e1b632 origin/feat/tokenizer`：20 文件全在 `大模型基础课/`、不碰 README/docs/演进路线/gitignore → 干净 `--no-ff`、无冲突。

**遗留 / 下一步**
- 新窗口按派发文字启动第二课；产出走"自己分支 commit → 回执统筹 → 统筹 --no-ff 集成"。
- 提醒 Tokenizer 窗口：第二课(位置编码)归新窗口，勿做重复的位置编码内容，专注第一课后续。

**分支 / PR**
- 合并 `feat/tokenizer`（基于 2e1b632）进 main，独立临时 worktree `--no-ff`；同集成里更新 README + 规矩 + 本条日志。

---

## 2026-08-20 · 集成演进路线 v1 + 两线迁入独立分支/worktree

**做了什么**
- 集成「大模型演进路线」v1：合并 `feat/roadmap-8companies`（3 commit、17 文件、14 页）进 main——8 家详情页（GPT/Claude/Gemini/Llama/Qwen/DeepSeek/Kimi/豆包）+ 索引 + 跨家族交互时间线 + 技术/指标/数据/来源专题页 + assets。
- 更新根 README：演进路线状态「建设中」→「v1 完成（14 页）」。
- 两条线各自迁入独立分支/worktree（各自掌管工作区）：演进路线用 `feat/roadmap-8companies`（已 push origin）；基础课用 `feat/tokenizer`（本地，内容 == main 暂无可合，有新提交再 push）。

**关键决策 / 教训**
- **根 README 由统筹集中维护**：本轮实际发生过一次撞车——演进路线窗口在共享主仓树里手改了根 README 加自己索引行，与统筹已推到 main 的新 README 冲突。处理：让该窗口 `git restore README.md` 丢弃冗余改动，索引统一由统筹更新。规矩里"内容窗口不改根 README、状态变更报统筹"由此坐实。
- **跨会话可见性受限、且非全连通**：两个内容窗口互相看不见，且基础课窗口发不进统筹会话，只能经演进路线窗口中转回执。协调靠精确 session_id 投递 + 暗号 LLMCO-2026 识别。
- **合并全程用独立临时 worktree**，不碰共享主仓工作树（内含内容窗口 WIP），符合"main 只集成、别碰别人 WIP"。

**验证**
- `git diff --name-only origin/main origin/feat/roadmap-8companies`：17 文件全在 `大模型演进路线/`、不碰 README → 干净 `--no-ff` 合并、无冲突。
- 核验 `feat/tokenizer` 在 origin 不存在、无独有提交，确认基础课线本次无可合。

**遗留 / 下一步**
- 基础课线后续（收尾第一课 / 第二课）在 `feat/tokenizer` 上做，有产出再 push + 回执，由统筹集成。
- 各内容窗口若把 cwd 从共享主仓树迁到各自 worktree，主仓树即可退化为统筹专用集成树，彻底消除共享树互踩风险。

**分支 / PR**
- 合并 `feat/roadmap-8companies`（基于 a847a1b）进 main，独立临时 worktree `--no-ff`；同一集成里更新 README 状态 + 本条日志。

---

## 2026-08-20 · 统筹窗口上线 + 项目规范化脚手架

**做了什么**
- 新开"统筹窗口"（会话标题 `llm-coordinator-inbox`），承接 LLMLearning 全项目的任务派发、目录/文档规范、`main` 集成、跨窗口协调。
- 摸清现状：两条并行学习线——① `大模型基础课/第一课-Tokenizer/`（已到 v2，含 08 tiktoken 对比页）；② `大模型演进路线/`（把飞书文档《主流大模型发展脉络(8家)》转成 HTML 站点，起步阶段）。
- 学习并对齐本机通用规范（`projects/内部沟通/`：Git 规范 / worktree 模式 / 文档维护）。
- 与两个内容窗口建立联系（跨会话消息 + 暗号 LLMCO-2026），对齐"暂停各自 git 操作、等统一 worktree 方案"。
- 新增本轮脚手架：`docs/协作规矩.md`、`docs/日志/开发日志.md`、`docs/计划/`、`docs/调研/`。

**关键决策 / 教训**
- **顶层目录保持平级两目录**（`大模型基础课/` + `大模型演进路线/`），不做统一父目录归拢——两条线目录不重叠即天然解耦，改动最小、并行最安全（用户拍板）。
- **统筹 ≠ 指挥**：内容窗口按权限边界只对接各自用户，统筹窗口只"提议约定 + 集成 main"，采纳与否由各窗口用户点头。这是权限按会话隔离的正确姿态。
- **跨会话可见性受限、slug 名是观察者相对的**：两个内容窗口互相看不见，且看统筹窗口的名字各不相同（`llmlearning-2f` / `llmlearning-7b`）。协调靠精确 session_id 投递 + 暗号识别，不靠名字。
- 脚手架在独立 worktree/分支（`chore/project-scaffold`）里做、再集成 main，示范合规流程；`docs/` 与两条学习线目录不重叠，合并无冲突。

**验证**
- `git worktree list` 确认 worktree 建在统一路径 `projects/worktrees/LLMLearning/`。
- `.gitignore` 已忽略 `.DS_Store`（根 + `**/`），无被跟踪的 .DS_Store。
- 演进脉络窗口已回执确认：只在 `大模型演进路线/` 下写、暂停 git、与 Tokenizer 线无文件重叠。

**遗留 / 下一步**
- 等 Tokenizer 窗口回执（确认在编辑的文件、是否迁入独立 worktree）。
- 两条学习线是否各自迁入独立 feature 分支/worktree，待各窗口用户点头后由统筹窗口建好派发。
- 本轮脚手架（`chore/project-scaffold`）经用户审阅后集成进 `main`。

**分支 / PR**
- 分支 `chore/project-scaffold`（基于 `origin/main` e67f8de）；集成方式：独立临时 worktree `--no-ff` 合并 main。
